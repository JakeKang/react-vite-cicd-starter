# Environment Variables Example
# 이 파일을 복사하여 .env.local 파일을 생성하세요
# cp .env.example .env.local

# =====================================
# API 설정
# =====================================
VITE_API_URL=http://localhost:4000

# =====================================
# 마이크로 앱 URL (Main App에서만 필요)
# =====================================
VITE_FEATURE_A_URL=http://localhost:3001
VITE_FEATURE_B_URL=http://localhost:3002
VITE_FEATURE_C_URL=http://localhost:3003

# =====================================
# Base Path (각 앱별로 다르게 설정)
# =====================================
# Main App: 설정하지 않음 (기본값 /)
# Feature A: VITE_BASE_PATH=/feature-a
# Feature B: VITE_BASE_PATH=/feature-b
# Feature C: VITE_BASE_PATH=/feature-c
VITE_BASE_PATH=

# =====================================
# 기타 설정
# =====================================
VITE_APP_NAME=main-app
VITE_APP_VERSION=1.0.0

# =====================================
# 주의사항
# =====================================
# 1. 모든 브라우저 노출 변수는 VITE_ prefix 필수
# 2. 민감한 정보(API 키, 비밀번호)는 절대 VITE_ 변수로 설정하지 말 것
# 3. 프로덕션 환경에서는 Docker build args로 주입
# 4. .env.local 파일은 .gitignore에 등록되어 있어야 함
